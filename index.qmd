---
title: "AI 기술 주기율표"
subtitle: "인터랙티브 탐색 도구"
format:
  dashboard:
    theme: cosmo
    orientation: columns
    scrolling: true
execute:
  echo: false
---

```{ojs}
//| output: false

// AI 원소 데이터 정의
elements = [
  // 원자(Atomic) - 실행
  {symbol: "Pr", name_kr: "프롬프트", name_en: "Prompts", group: "실행", period: "원자", row: 1, col: 1,
   desc: "AI와 소통하는 자연어 지시. 모든 AI 상호작용의 시작점.",
   examples: ["ChatGPT 프롬프트", "시스템 프롬프트", "Few-shot 예제"]},

  // 원자(Atomic) - 지식
  {symbol: "Em", name_kr: "임베딩", name_en: "Embeddings", group: "지식", period: "원자", row: 1, col: 2,
   desc: "텍스트를 고차원 벡터로 변환. 의미를 수치화하여 검색과 비교 가능하게 함.",
   examples: ["OpenAI text-embedding-3", "sentence-transformers", "Cohere Embed"]},

  // 원자(Atomic) - 구조
  {symbol: "Me", name_kr: "메모리", name_en: "Memory", group: "구조", period: "원자", row: 1, col: 3,
   desc: "대화 맥락과 상태 유지. Agent가 과거 상호작용을 기억하게 함.",
   examples: ["대화 히스토리", "작업 메모리", "장기 메모리"]},

  // 원자(Atomic) - 안전
  {symbol: "Ev", name_kr: "평가", name_en: "Evaluations", group: "안전", period: "원자", row: 1, col: 4,
   desc: "모델 성능 측정 및 벤치마크. 품질 보증의 기초.",
   examples: ["BLEU", "ROUGE", "HumanEval", "MMLU"]},

  // 원자(Atomic) - 모델
  {symbol: "Lg", name_kr: "대형 언어모델", name_en: "LLM", group: "모델", period: "원자", row: 1, col: 5,
   desc: "텍스트 생성 및 이해. AI 시스템의 핵심 추론 엔진.",
   examples: ["GPT-4o", "Claude Opus 4", "Llama 4", "Gemini 2.5"]},

  // 분자(Molecular) - 실행
  {symbol: "Fc", name_kr: "함수 호출", name_en: "Function Call", group: "실행", period: "분자", row: 2, col: 1,
   desc: "LLM이 외부 도구(API, DB, 계산기)를 호출. Agent의 핵심 메커니즘.",
   examples: ["OpenAI tools", "Anthropic tool_use", "날씨 API", "계산기"]},

  // 분자(Molecular) - 지식
  {symbol: "Vx", name_kr: "벡터 DB", name_en: "Vector DB", group: "지식", period: "분자", row: 2, col: 2,
   desc: "고차원 벡터 저장 및 유사도 검색. RAG의 핵심 저장소.",
   examples: ["Pinecone", "Chroma", "Qdrant", "pgvector"]},

  // 분자(Molecular) - 구조
  {symbol: "Rg", name_kr: "검색 증강 생성", name_en: "RAG", group: "구조", period: "분자", row: 2, col: 3,
   desc: "외부 지식 검색 + LLM 생성. 환각 문제 해결.",
   examples: ["문서 Q&A", "사내 지식 검색", "법률 자문"]},

  // 분자(Molecular) - 안전
  {symbol: "Gr", name_kr: "가드레일", name_en: "Guardrails", group: "안전", period: "분자", row: 2, col: 4,
   desc: "유해 콘텐츠 필터링, 프롬프트 인젝션 방어, 출력 검증.",
   examples: ["NVIDIA NeMo", "Guardrails AI", "LLM Guard"]},

  // 분자(Molecular) - 모델
  {symbol: "Mm", name_kr: "멀티모달", name_en: "Multimodal", group: "모델", period: "분자", row: 2, col: 5,
   desc: "텍스트, 이미지, 오디오 통합 처리. 시각+언어 능력.",
   examples: ["GPT-4 Vision", "Claude Opus 4", "Gemini 2.5 Pro"]},

  // 시스템(System) - 실행
  {symbol: "Ag", name_kr: "에이전트", name_en: "Agent", group: "실행", period: "시스템", row: 3, col: 1,
   desc: "자율적 작업 수행. 목표 설정 → 계획 → 실행 → 검증 루프.",
   examples: ["Claude Code", "Cursor Composer", "Aider", "AutoGPT"]},

  // 시스템(System) - 지식
  {symbol: "Ft", name_kr: "파인튜닝", name_en: "Fine-tuning", group: "지식", period: "시스템", row: 3, col: 2,
   desc: "특정 도메인에 맞춰 모델 재학습. 지식 주입.",
   examples: ["LoRA", "QLoRA", "Full Fine-tune", "DPO"]},

  // 시스템(System) - 구조
  {symbol: "Fw", name_kr: "프레임워크", name_en: "Framework", group: "구조", period: "시스템", row: 3, col: 3,
   desc: "AI 구성 요소 통합 및 오케스트레이션. 복잡한 워크플로우 관리.",
   examples: ["LangChain", "LlamaIndex", "LangGraph", "Semantic Kernel"]},

  // 시스템(System) - 안전
  {symbol: "Rt", name_kr: "레드팀", name_en: "Red-teaming", group: "안전", period: "시스템", row: 3, col: 4,
   desc: "적대적 공격으로 취약점 발견. 보안 감사.",
   examples: ["Jailbreak 테스트", "프롬프트 인젝션", "유해 콘텐츠 생성"]},

  // 시스템(System) - 모델
  {symbol: "Sm", name_kr: "소형 모델", name_en: "Small Models", group: "모델", period: "시스템", row: 3, col: 5,
   desc: "경량화 모델(1-10B). 엣지 디바이스, 저비용 배포.",
   examples: ["Phi-3", "Gemma 7B", "Llama 3.2 3B", "Qwen 2.5 7B"]},

  // 프런티어(Frontier) - 실행
  {symbol: "Ma", name_kr: "멀티 에이전트", name_en: "Multi-agent", group: "실행", period: "프런티어", row: 4, col: 1,
   desc: "여러 Agent 협업. 복잡한 작업을 역할 분담하여 해결.",
   examples: ["AutoGen", "CrewAI", "MetaGPT"]},

  // 프런티어(Frontier) - 지식
  {symbol: "Sy", name_kr: "합성 데이터", name_en: "Synthetic Data", group: "지식", period: "프런티어", row: 4, col: 2,
   desc: "AI가 학습 데이터를 생성. 데이터 부족 문제 해결.",
   examples: ["Self-Instruct", "Evol-Instruct", "WizardLM"]},

  // 프런티어(Frontier) - 구조
  {symbol: "Op", name_kr: "최적화기", name_en: "Optimizers", group: "구조", period: "프런티어", row: 4, col: 3,
   desc: "자동 프롬프트 최적화. 메타 학습.",
   examples: ["DSPy", "Prompt Optimization", "AutoPrompt"]},

  // 프런티어(Frontier) - 안전
  {symbol: "In", name_kr: "해석가능성", name_en: "Interpretability", group: "안전", period: "프런티어", row: 4, col: 4,
   desc: "모델 내부 작동 이해. 블랙박스 해체.",
   examples: ["Attention 시각화", "SHAP", "LIME", "Mechanistic Interpretability"]},

  // 프런티어(Frontier) - 모델
  {symbol: "Th", name_kr: "추론 모델", name_en: "Reasoning", group: "모델", period: "프런티어", row: 4, col: 5,
   desc: "단계적 사고(CoT). 복잡한 논리 문제 해결.",
   examples: ["OpenAI o1", "o3", "DeepSeek-R1"]}
];

// 인프라 원소
infra_elements = [
  {symbol: "Mo", name_kr: "모니터링", name_en: "Monitoring", group: "인프라", period: "인프라",
   desc: "시스템 성능, 비용, 품질 추적. 지속적 개선의 기초.",
   examples: ["LangSmith", "Weights & Biases", "Prometheus", "AgentOps"]},

  {symbol: "Sv", name_kr: "서빙", name_en: "Serving", group: "인프라", period: "인프라",
   desc: "모델 배포 및 추론 서버. 프로덕션 운영.",
   examples: ["vLLM", "TensorRT-LLM", "SGLang", "TGI"]},

  {symbol: "Qz", name_kr: "양자화", name_en: "Quantization", group: "인프라", period: "인프라",
   desc: "모델 경량화(16bit→4bit). 메모리 절감.",
   examples: ["GPTQ", "AWQ", "GGUF", "bitsandbytes"]},

  {symbol: "Gp", name_kr: "연산 장치", name_en: "Compute/GPU", group: "인프라", period: "인프라",
   desc: "하드웨어 인프라. GPU, TPU, NPU.",
   examples: ["NVIDIA B200", "H200", "AMD MI300X", "Google TPU v5"]}
];

// 화학식 데이터
formulas = [
  {
    name: "RAG (검색 증강 생성)",
    formula: "Pr + Em + Vx + Lg",
    elements_used: ["Pr", "Em", "Vx", "Lg"],
    category: "지식",
    description: "외부 지식을 검색하여 LLM 답변에 통합. 환각 문제 해결.",
    use_cases: ["기업 지식 Q&A", "문서 검색", "법률/의료 자문"],
    example_impl: "LlamaIndex + OpenAI + Pinecone"
  },
  {
    name: "자율 에이전트",
    formula: "Pr + Me + Fc + Th",
    elements_used: ["Pr", "Me", "Fc", "Th"],
    category: "실행",
    description: "목표 설정 → 계획 → 도구 사용 → 실행. 복잡한 작업 자율 수행.",
    use_cases: ["소프트웨어 개발", "데이터 분석", "자동화 워크플로우"],
    example_impl: "AutoGPT + OpenAI + LangChain"
  },
  {
    name: "멀티 에이전트 협업",
    formula: "Ma + Pr + Fc + Lg",
    elements_used: ["Ma", "Pr", "Fc", "Lg"],
    category: "실행",
    description: "여러 에이전트가 역할 분담하여 복잡한 프로젝트 수행.",
    use_cases: ["소프트웨어 팀 시뮬레이션", "연구 자동화", "컨텐츠 제작"],
    example_impl: "CrewAI + GPT-4 + Composio"
  },
  {
    name: "안전한 프로덕션 RAG",
    formula: "Pr + Em + Vx + Lg + Gr + Ev",
    elements_used: ["Pr", "Em", "Vx", "Lg", "Gr", "Ev"],
    category: "지식",
    description: "RAG + 안전장치 + 평가. 엔터프라이즈용 완전체.",
    use_cases: ["금융 챗봇", "의료 상담", "규제 산업 Q&A"],
    example_impl: "Haystack + Claude + NeMo Guardrails + LangSmith"
  },
  {
    name: "코딩 에이전트",
    formula: "Ag + Fc + Me + Lg + Ev",
    elements_used: ["Ag", "Fc", "Me", "Lg", "Ev"],
    category: "실행",
    description: "코드 작성 + 파일 접근 + 테스트 실행. 개발 자동화.",
    use_cases: ["자동 코드 생성", "버그 수정", "리팩토링"],
    example_impl: "Claude Code + Anthropic MCP"
  },
  {
    name: "파인튜닝 파이프라인",
    formula: "Ft + Lg + Ev + Gp",
    elements_used: ["Ft", "Lg", "Ev", "Gp"],
    category: "모델",
    description: "도메인 특화 모델 학습. 데이터 → 학습 → 평가.",
    use_cases: ["고객 서비스 봇", "법률 문서 분석", "의료 진단 보조"],
    example_impl: "Unsloth + Llama 3 + W&B + H200"
  },
  {
    name: "멀티모달 앱",
    formula: "Mm + Pr + Fc + Me",
    elements_used: ["Mm", "Pr", "Fc", "Me"],
    category: "모델",
    description: "이미지+텍스트 통합 처리. 시각적 이해 + 대화.",
    use_cases: ["이미지 설명", "문서 OCR", "비디오 분석"],
    example_impl: "GPT-4V + OpenAI API"
  },
  {
    name: "경량 온디바이스 AI",
    formula: "Sm + Qz + Pr",
    elements_used: ["Sm", "Qz", "Pr"],
    category: "모델",
    description: "소형 모델 + 양자화. 모바일/엣지 디바이스 배포.",
    use_cases: ["스마트폰 AI", "IoT 디바이스", "오프라인 앱"],
    example_impl: "Phi-3 + GGUF + Ollama"
  }
];

// 동위원소 데이터
isotopes = [
  {
    element: "Lg",
    name: "LLM (대형 언어모델)",
    variants: [
      {symbol: "GPT-Lg", provider: "OpenAI", models: ["GPT-5.2", "GPT-4o", "o1", "o3"], strength: "넓은 생태계, API 안정성"},
      {symbol: "Cla-Lg", provider: "Anthropic", models: ["Claude Opus 4.5", "Sonnet 4.5"], strength: "긴 컨텍스트, 코딩"},
      {symbol: "Lla-Lg", provider: "Meta", models: ["Llama 4", "Llama 3.3"], strength: "오픈소스, 온프레미스"},
      {symbol: "Gem-Lg", provider: "Google", models: ["Gemini 3 Pro", "Gemini 2.5"], strength: "멀티모달, 1M 토큰"}
    ]
  },
  {
    element: "Em",
    name: "임베딩",
    variants: [
      {symbol: "OAI-Em", provider: "OpenAI", models: ["text-embedding-3-large", "text-embedding-3-small"], strength: "RAG 최적화, 다국어"},
      {symbol: "Coh-Em", provider: "Cohere", models: ["embed-v4"], strength: "MTEB 1위, 장문 지원"},
      {symbol: "Voy-Em", provider: "Voyage AI", models: ["voyage-3", "voyage-code-3"], strength: "코드 특화, 고성능"},
      {symbol: "BGE-Em", provider: "BAAI", models: ["BGE-M3"], strength: "오픈소스, 무료"}
    ]
  },
  {
    element: "Pr",
    name: "프롬프트",
    variants: [
      {symbol: "GPT-Pr", provider: "OpenAI", models: ["ChatGPT", "Playground"], strength: "대중적, 사용 편의성"},
      {symbol: "Cla-Pr", provider: "Anthropic", models: ["Claude.ai", "Prompt Generator"], strength: "프롬프트 최적화 도구"},
      {symbol: "PB-Pr", provider: "PromptBuilder", models: ["PromptBuilder"], strength: "100+ 템플릿, 최적화 엔진"},
      {symbol: "PP-Pr", provider: "PromptPerfect", models: ["PromptPerfect"], strength: "자동 튜닝, 멀티모델"}
    ]
  },
  {
    element: "Vx",
    name: "벡터 DB",
    variants: [
      {symbol: "Pin-Vx", provider: "Pinecone", models: ["Pinecone"], strength: "완전 관리형 SaaS"},
      {symbol: "Chr-Vx", provider: "Chroma", models: ["Chroma"], strength: "오픈소스, 로컬 개발"},
      {symbol: "Pg-Vx", provider: "pgvector", models: ["PostgreSQL 확장"], strength: "기존 인프라 활용"},
      {symbol: "Qd-Vx", provider: "Qdrant", models: ["Qdrant"], strength: "고성능, 필터링"}
    ]
  },
  {
    element: "Me",
    name: "메모리",
    variants: [
      {symbol: "MCP-Me", provider: "Anthropic", models: ["MCP Memory"], strength: "컨텍스트 공유, 표준화"},
      {symbol: "Mem-Me", provider: "Memories.ai", models: ["Project LUCI"], strength: "시각 메모리, CES 2026"},
      {symbol: "LC-Me", provider: "LangChain", models: ["ConversationBufferMemory"], strength: "대화 히스토리 관리"},
      {symbol: "Zep-Me", provider: "Zep", models: ["Zep Memory"], strength: "장기 메모리, RAG 통합"}
    ]
  },
  {
    element: "Fc",
    name: "함수 호출",
    variants: [
      {symbol: "OAI-Fc", provider: "OpenAI", models: ["Function Calling API"], strength: "스키마 검증, 구조화"},
      {symbol: "Ant-Fc", provider: "Anthropic", models: ["Tool Use", "MCP"], strength: "유연한 도구 체이닝"},
      {symbol: "Com-Fc", provider: "Composio", models: ["Composio"], strength: "100+ 도구 통합"},
      {symbol: "Ver-Fc", provider: "Vercel", models: ["AI SDK"], strength: "30+ LLM 표준화"}
    ]
  },
  {
    element: "Rg",
    name: "검색 증강 생성 (RAG)",
    variants: [
      {symbol: "LI-Rg", provider: "LlamaIndex", models: ["LlamaIndex"], strength: "RAG 특화, 데이터 커넥터"},
      {symbol: "LC-Rg", provider: "LangChain", models: ["LangChain RAG"], strength: "포괄적 생태계"},
      {symbol: "Hay-Rg", provider: "Haystack", models: ["Haystack"], strength: "프로덕션 그레이드, 99.9% 가동률"},
      {symbol: "DSP-Rg", provider: "Stanford", models: ["DSPy"], strength: "프롬프트 최적화"}
    ]
  },
  {
    element: "Gr",
    name: "가드레일",
    variants: [
      {symbol: "NM-Gr", provider: "NVIDIA", models: ["NeMo Guardrails"], strength: "저지연, GPU 가속"},
      {symbol: "GA-Gr", provider: "Guardrails AI", models: ["Guardrails AI"], strength: "PII 검출, 독성 필터"},
      {symbol: "Cis-Gr", provider: "Cisco", models: ["AI Defense"], strength: "엔터프라이즈 보안"},
      {symbol: "AWS-Gr", provider: "AWS", models: ["Bedrock Guardrails"], strength: "AWS 통합"}
    ]
  },
  {
    element: "Mm",
    name: "멀티모달",
    variants: [
      {symbol: "Gem-Mm", provider: "Google", models: ["Gemini 3 Pro"], strength: "네이티브 멀티모달"},
      {symbol: "GPT-Mm", provider: "OpenAI", models: ["GPT-4V", "GPT-5.2"], strength: "이미지+비디오 분석"},
      {symbol: "Cla-Mm", provider: "Anthropic", models: ["Claude 4"], strength: "컴퓨터 사용, 이미지"},
      {symbol: "Qw-Mm", provider: "Alibaba", models: ["Qwen-VL"], strength: "오픈소스, 시각+언어"}
    ]
  },
  {
    element: "Ag",
    name: "에이전트",
    variants: [
      {symbol: "CC-Ag", provider: "Anthropic", models: ["Claude Code"], strength: "코딩 에이전트"},
      {symbol: "Cur-Ag", provider: "Cursor", models: ["Cursor Composer"], strength: "IDE 통합"},
      {symbol: "Aid-Ag", provider: "Aider", models: ["Aider"], strength: "Git 워크플로우"},
      {symbol: "AGP-Ag", provider: "오픈소스", models: ["AutoGPT"], strength: "자율 목표 달성"}
    ]
  },
  {
    element: "Ev",
    name: "평가",
    variants: [
      {symbol: "LS-Ev", provider: "LangChain", models: ["LangSmith"], strength: "LangChain 통합, 추적"},
      {symbol: "WB-Ev", provider: "W&B", models: ["Weave"], strength: "MLOps, 실험 추적"},
      {symbol: "Gal-Ev", provider: "Galileo", models: ["Galileo"], strength: "GenAI 평가 특화"},
      {symbol: "Ari-Ev", provider: "Arize", models: ["Arize Phoenix"], strength: "모니터링, 관찰성"}
    ]
  },
  {
    element: "Ft",
    name: "파인튜닝",
    variants: [
      {symbol: "LoR-Ft", provider: "HuggingFace", models: ["LoRA", "PEFT"], strength: "90% 메모리 절감"},
      {symbol: "QLo-Ft", provider: "QLoRA", models: ["QLoRA", "4-bit"], strength: "초고효율, 4비트"},
      {symbol: "Uns-Ft", provider: "Unsloth", models: ["Unsloth"], strength: "2배 빠름, 소비자 GPU"},
      {symbol: "Tog-Ft", provider: "Together AI", models: ["Together Finetune"], strength: "관리형 서비스"}
    ]
  },
  {
    element: "Ma",
    name: "멀티 에이전트",
    variants: [
      {symbol: "Crew-Ma", provider: "CrewAI", models: ["CrewAI"], strength: "역할 기반 팀워크"},
      {symbol: "Auto-Ma", provider: "Microsoft", models: ["AutoGen"], strength: "대화형 협업, HIL"},
      {symbol: "Meta-Ma", provider: "MetaGPT", models: ["MetaGPT"], strength: "소프트웨어 팀 시뮬"},
      {symbol: "LG-Ma", provider: "LangChain", models: ["LangGraph Multi"], strength: "상태 그래프 오케스트"}
    ]
  },
  {
    element: "Sm",
    name: "소형 모델",
    variants: [
      {symbol: "Phi-Sm", provider: "Microsoft", models: ["Phi-4", "Phi-3"], strength: "3.8B, 추론 특화"},
      {symbol: "Gem-Sm", provider: "Google", models: ["Gemma 3", "Gemma 2"], strength: "2B-27B, 모바일 최적화"},
      {symbol: "Lla-Sm", provider: "Meta", models: ["Llama 3.2 1B/3B"], strength: "온디바이스 AI"},
      {symbol: "Qw-Sm", provider: "Alibaba", models: ["Qwen 2.5 7B"], strength: "오픈소스, 다국어"}
    ]
  },
  {
    element: "Fw",
    name: "프레임워크",
    variants: [
      {symbol: "LC-Fw", provider: "LangChain", models: ["LangChain"], strength: "가장 인기, 100+ 도구"},
      {symbol: "LI-Fw", provider: "LlamaIndex", models: ["LlamaIndex"], strength: "RAG 특화, 데이터 커넥터"},
      {symbol: "LG-Fw", provider: "LangGraph", models: ["LangGraph"], strength: "그래프 기반, 조건 분기"},
      {symbol: "SK-Fw", provider: "Semantic Kernel", models: ["Microsoft SK"], strength: "C#/Python, 엔터프라이즈"}
    ]
  },
  {
    element: "Mo",
    name: "모니터링",
    variants: [
      {symbol: "LS-Mo", provider: "LangChain", models: ["LangSmith"], strength: "LLM 추적, 무부하"},
      {symbol: "WB-Mo", provider: "W&B", models: ["Weave"], strength: "@weave.op 데코레이터"},
      {symbol: "Prom-Mo", provider: "Prometheus", models: ["Prometheus+Grafana"], strength: "메트릭 수집, 시각화"},
      {symbol: "AgOp-Mo", provider: "AgentOps", models: ["AgentOps"], strength: "에이전트 특화 관찰"}
    ]
  },
  {
    element: "Sv",
    name: "서빙",
    variants: [
      {symbol: "vLLM-Sv", provider: "UC Berkeley", models: ["vLLM"], strength: "PagedAttention, 범용"},
      {symbol: "TRT-Sv", provider: "NVIDIA", models: ["TensorRT-LLM"], strength: "최고 성능, H100"},
      {symbol: "SGL-Sv", provider: "SGLang", models: ["SGLang"], strength: "RadixAttention, 3배 빠름"},
      {symbol: "TGI-Sv", provider: "HuggingFace", models: ["TGI"], strength: "장문 처리, 200K 토큰"}
    ]
  },
  {
    element: "Qz",
    name: "양자화",
    variants: [
      {symbol: "GPTQ-Qz", provider: "GPTQ", models: ["GPTQ"], strength: "GPU 특화, 75% 압축"},
      {symbol: "AWQ-Qz", provider: "MIT", models: ["AWQ"], strength: "활성화 인식, 고정밀도"},
      {symbol: "GGUF-Qz", provider: "llama.cpp", models: ["GGUF"], strength: "CPU/Apple M 최적화"},
      {symbol: "BnB-Qz", provider: "bitsandbytes", models: ["bitsandbytes"], strength: "4/8비트, QLoRA"}
    ]
  },
  {
    element: "Gp",
    name: "연산 장치",
    variants: [
      {symbol: "B200-Gp", provider: "NVIDIA", models: ["B200 Blackwell"], strength: "H100 대비 4배 성능"},
      {symbol: "H200-Gp", provider: "NVIDIA", models: ["H200"], strength: "141GB HBM3e, 최고 처리량"},
      {symbol: "MI3-Gp", provider: "AMD", models: ["MI300X", "MI325X"], strength: "192GB, H200의 74%"},
      {symbol: "TPU-Gp", provider: "Google", models: ["TPU v5", "v6e"], strength: "Google 생태계"}
    ]
  }
];

// 선택된 원소를 저장할 mutable 변수
mutable selectedElement = "Pr";

// 드롭다운 selector
viewof elementDropdown = Inputs.select(
  elements.map(e => ({label: `${e.symbol} - ${e.name_kr}`, value: e.symbol})),
  {label: "원소 선택", value: "Pr"}
);

// 드롭다운 변경 감지
elementDropdownSync = {
  mutable selectedElement = elementDropdown;
  return elementDropdown;
};

// 현재 선택된 원소 데이터 (일반 원소와 인프라 원소 모두 검색)
currentElement = elements.find(e => e.symbol === selectedElement) ||
                 infra_elements.find(e => e.symbol === selectedElement) ||
                 elements[0];

// 선택된 화학식 인덱스 (mutable)
mutable selectedFormulaIndex = 0;

// 현재 선택된 화학식
currentFormula = formulas[selectedFormulaIndex] || formulas[0];
```

# 사용법 {scrolling="true"}

## Column

### AI 주기율표란?

```{ojs}
html`<div style="padding: 30px; max-width: 900px; margin: 0 auto;">
  <h2 style="color: #222; font-family: Palatino, Georgia, serif; margin-bottom: 20px;">
    AI 기술을 주기율표로 이해하기
  </h2>

  <div style="background: #f9f9f9; padding: 20px; border-radius: 5px; margin: 20px 0;">
    <p style="font-size: 15px; line-height: 1.8; color: #333;">
      화학의 주기율표가 원소들을 체계적으로 분류하듯이, <strong>AI 주기율표</strong>는
      현대 AI 기술 스택의 핵심 구성요소들을 5개 족(Group)과 4개 주기(Period)로 체계화합니다.
    </p>
  </div>

  <h3 style="color: #333; margin-top: 30px;">📊 주기율표 구조</h3>

  <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin: 20px 0;">
    <div style="background: white; padding: 20px; border-radius: 5px; border-left: 4px solid #0066cc;">
      <h4 style="margin-top: 0; color: #0066cc;">족 (Groups) - 역할별 분류</h4>
      <ul style="line-height: 1.8; color: #555;">
        <li><strong>실행 (Action)</strong>: 프롬프트, 함수 호출, 에이전트</li>
        <li><strong>지식 (Knowledge)</strong>: 임베딩, 벡터 DB, 합성 데이터</li>
        <li><strong>구조 (Structure)</strong>: 메모리, RAG, 프레임워크</li>
        <li><strong>안전 (Safety)</strong>: 평가, 가드레일, 레드팀</li>
        <li><strong>모델 (Model)</strong>: LLM, 멀티모달, 소형 모델</li>
      </ul>
    </div>

    <div style="background: white; padding: 20px; border-radius: 5px; border-left: 4px solid #ffa500;">
      <h4 style="margin-top: 0; color: #ffa500;">주기 (Periods) - 복잡도 단계</h4>
      <ul style="line-height: 1.8; color: #555;">
        <li><strong>원자 (Atomic)</strong>: 기본 구성 요소</li>
        <li><strong>분자 (Molecular)</strong>: 원자의 조합</li>
        <li><strong>시스템 (System)</strong>: 통합 솔루션</li>
        <li><strong>프런티어 (Frontier)</strong>: 최신 연구</li>
      </ul>
    </div>
  </div>

  <h3 style="color: #333; margin-top: 30px;">💡 사용 방법</h3>

  <div style="background: #fff8e6; padding: 20px; border-radius: 5px; margin: 20px 0;">
    <h4 style="margin-top: 0; color: #cc8800;">주기율표 탭</h4>
    <ol style="line-height: 1.8; color: #555;">
      <li>주기율표에서 <strong>원소를 클릭</strong>하면 상세 설명이 표시됩니다</li>
      <li>오른쪽 상단에 <strong>동위원소(구현체)</strong>가 자동으로 표시됩니다</li>
      <li>왼쪽 필터로 <strong>특정 족이나 주기</strong>만 볼 수 있습니다</li>
      <li>동위원소는 같은 역할을 하는 실제 제품/서비스입니다</li>
    </ol>
  </div>

  <div style="background: #e8f4f8; padding: 20px; border-radius: 5px; margin: 20px 0;">
    <h4 style="margin-top: 0; color: #0066cc;">화학식 탭</h4>
    <ol style="line-height: 1.8; color: #555;">
      <li>왼쪽 사이드바에서 <strong>AI 아키텍처 패턴</strong>을 선택합니다</li>
      <li>화학식은 여러 원소를 조합한 <strong>실용적인 솔루션</strong>입니다</li>
      <li>구성 원소 버튼을 클릭하면 <strong>주기율표 탭</strong>으로 이동합니다</li>
      <li>예시 구현에서 실제 기술 스택 조합을 확인할 수 있습니다</li>
    </ol>
  </div>

  <h3 style="color: #333; margin-top: 30px;">🔬 주요 개념</h3>

  <div style="background: white; padding: 20px; border-radius: 5px; border: 1px solid #ddd; margin: 20px 0;">
    <h4 style="color: #555;">동위원소 (Isotopes)</h4>
    <p style="line-height: 1.7; color: #666;">
      화학에서 동위원소는 같은 원소이지만 질량이 다른 변종입니다.
      AI 주기율표에서는 <strong>같은 역할을 수행하지만 프로바이더나 특성이 다른 구현체</strong>를 의미합니다.
    </p>
    <p style="line-height: 1.7; color: #666; margin-top: 10px;">
      예: LLM의 동위원소 → GPT-5.2 (OpenAI), Claude Opus 4.5 (Anthropic), Gemini 3 Pro (Google)
    </p>
  </div>

  <div style="background: white; padding: 20px; border-radius: 5px; border: 1px solid #ddd; margin: 20px 0;">
    <h4 style="color: #555;">화학식 (Formulas)</h4>
    <p style="line-height: 1.7; color: #666;">
      화학식이 원소들의 결합을 나타내듯이, AI 화학식은 <strong>여러 기술 요소를 조합한
      아키텍처 패턴</strong>을 표현합니다.
    </p>
    <p style="line-height: 1.7; color: #666; margin-top: 10px;">
      예: RAG = Pr (프롬프트) + Em (임베딩) + Vx (벡터 DB) + Lg (LLM)
    </p>
  </div>

  <h3 style="color: #333; margin-top: 30px;">📈 통계</h3>
  <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; margin: 20px 0;">
    <div style="background: #f0f0f0; padding: 15px; border-radius: 5px; text-align: center;">
      <div style="font-size: 32px; font-weight: bold; color: #0066cc;">${elements.length}</div>
      <div style="font-size: 14px; color: #666; margin-top: 5px;">주기율표 원소</div>
    </div>
    <div style="background: #f0f0f0; padding: 15px; border-radius: 5px; text-align: center;">
      <div style="font-size: 32px; font-weight: bold; color: #ffa500;">${infra_elements.length}</div>
      <div style="font-size: 14px; color: #666; margin-top: 5px;">인프라 원소</div>
    </div>
    <div style="background: #f0f0f0; padding: 15px; border-radius: 5px; text-align: center;">
      <div style="font-size: 32px; font-weight: bold; color: #28a745;">${formulas.length}</div>
      <div style="font-size: 14px; color: #666; margin-top: 5px;">화학식 패턴</div>
    </div>
  </div>

  <div style="background: #e8f4f8; padding: 20px; border-radius: 5px; margin-top: 30px;">
    <p style="font-size: 14px; line-height: 1.7; color: #555; margin: 0;">
      💡 <strong>팁:</strong> 각 원소를 클릭하면 2026년 기준 최신 구현체(동위원소) 정보를 확인할 수 있습니다.
      80%의 원소에 실제 제품/서비스 정보가 등록되어 있습니다.
    </p>
  </div>
</div>`
```

# 주기율표 {scrolling="true"}

## {.sidebar}

```{ojs}
html`<div style="padding: 20px 15px; font-family: 'Palatino', 'Georgia', serif;">
  <div style="margin-bottom: 35px; border-bottom: 1px solid #ddd; padding-bottom: 15px;">
    <h3 style="
      margin: 0 0 5px 0;
      font-size: 13px;
      font-weight: 400;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      color: #999;
    ">Filter by</h3>
  </div>
</div>`
```

```{ojs}
html`<div style="padding: 0 15px 25px 15px; font-family: 'Palatino', 'Georgia', serif;">
  <h4 style="
    margin: 0 0 12px 0;
    font-size: 14px;
    font-weight: 600;
    color: #333;
    letter-spacing: -0.2px;
  ">족 (Group)</h4>
</div>`
```

```{ojs}
{
  const container = d3.create("div")
    .style("padding", "0 15px 30px 15px");

  const groups = [
    {value: "전체", label: "All"},
    {value: "실행", label: "Action", color: "#0066cc"},
    {value: "지식", label: "Knowledge", color: "#28a745"},
    {value: "구조", label: "Structure", color: "#ffa500"},
    {value: "안전", label: "Safety", color: "#dc3545"},
    {value: "모델", label: "Model", color: "#6f42c1"}
  ];

  groups.forEach(g => {
    const item = container.append("div")
      .style("margin-bottom", "8px")
      .style("cursor", "pointer")
      .style("display", "flex")
      .style("align-items", "center")
      .style("padding", "8px 10px")
      .style("border-radius", "3px")
      .style("background", filterGroup === g.value ? "#f5f5f5" : "transparent")
      .style("transition", "background 0.15s")
      .on("click", () => {
        viewof filterGroup.value = g.value;
        viewof filterGroup.dispatchEvent(new Event("input"));
      })
      .on("mouseover", function() {
        if (filterGroup !== g.value) {
          d3.select(this).style("background", "#fafafa");
        }
      })
      .on("mouseout", function() {
        if (filterGroup !== g.value) {
          d3.select(this).style("background", "transparent");
        }
      });

    if (g.color) {
      item.append("div")
        .style("width", "3px")
        .style("height", "16px")
        .style("background", g.color)
        .style("margin-right", "10px")
        .style("border-radius", "1px");
    }

    item.append("span")
      .style("font-size", "13px")
      .style("color", filterGroup === g.value ? "#222" : "#555")
      .style("font-weight", filterGroup === g.value ? "600" : "400")
      .text(g.value);

    if (g.label && g.value !== "전체") {
      item.append("span")
        .style("font-size", "11px")
        .style("color", "#999")
        .style("margin-left", "auto")
        .style("font-style", "italic")
        .text(g.label);
    }
  });

  return container.node();
}
```

```{ojs}
html`<div style="padding: 0 15px 25px 15px; font-family: 'Palatino', 'Georgia', serif; margin-top: 20px;">
  <h4 style="
    margin: 0 0 12px 0;
    font-size: 14px;
    font-weight: 600;
    color: #333;
    letter-spacing: -0.2px;
  ">주기 (Period)</h4>
</div>`
```

```{ojs}
{
  const container = d3.create("div")
    .style("padding", "0 15px");

  const periods = [
    {value: "전체", label: "All"},
    {value: "원자", label: "Atomic"},
    {value: "분자", label: "Molecular"},
    {value: "시스템", label: "System"},
    {value: "프런티어", label: "Frontier"}
  ];

  periods.forEach(p => {
    const item = container.append("div")
      .style("margin-bottom", "8px")
      .style("cursor", "pointer")
      .style("padding", "8px 10px")
      .style("border-radius", "3px")
      .style("background", filterPeriod === p.value ? "#f5f5f5" : "transparent")
      .style("transition", "background 0.15s")
      .on("click", () => {
        viewof filterPeriod.value = p.value;
        viewof filterPeriod.dispatchEvent(new Event("input"));
      })
      .on("mouseover", function() {
        if (filterPeriod !== p.value) {
          d3.select(this).style("background", "#fafafa");
        }
      })
      .on("mouseout", function() {
        if (filterPeriod !== p.value) {
          d3.select(this).style("background", "transparent");
        }
      });

    item.append("span")
      .style("font-size", "13px")
      .style("color", filterPeriod === p.value ? "#222" : "#555")
      .style("font-weight", filterPeriod === p.value ? "600" : "400")
      .text(p.value);

    if (p.label && p.value !== "전체") {
      item.append("span")
        .style("font-size", "11px")
        .style("color", "#999")
        .style("margin-left", "10px")
        .style("font-style", "italic")
        .text(p.label);
    }
  });

  return container.node();
}
```

```{ojs}
// 필터 입력 (숨김)
viewof filterGroup = Inputs.input("전체");
viewof filterPeriod = Inputs.input("전체");
```

## Column {width="70%"}

### 주기율표

```{ojs}
{
  const width = 950;
  const height = 680;
  const cellWidth = 150;
  const cellHeight = 100;

  const svg = d3.create("svg")
    .attr("width", "100%")
    .attr("height", "100%")
    .attr("viewBox", [0, 0, width, height])
    .attr("preserveAspectRatio", "xMidYMid meet")
    .style("background", "#fff")
    .style("max-width", "100%")
    .style("height", "auto");

  // 컬럼 헤더 (Groups)
  const groups = ["실행", "지식", "구조", "안전", "모델"];
  const groupsEn = ["Action", "Knowledge", "Structure", "Safety", "Model"];

  groups.forEach((g, i) => {
    const x = 150 + i * (cellWidth + 10);
    svg.append("text")
      .attr("x", x + cellWidth/2)
      .attr("y", 35)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "14px")
      .style("font-weight", "bold")
      .style("fill", "#333")
      .text(g);

    svg.append("text")
      .attr("x", x + cellWidth/2)
      .attr("y", 50)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "10px")
      .style("font-style", "italic")
      .style("fill", "#777")
      .text(groupsEn[i]);
  });

  // 행 헤더 (Periods)
  const periods = ["원자", "분자", "시스템", "프런티어"];
  const periodsEn = ["Atomic", "Molecular", "System", "Frontier"];

  periods.forEach((p, i) => {
    const y = 80 + i * (cellHeight + 10);
    svg.append("text")
      .attr("x", 130)
      .attr("y", y + cellHeight/2 - 5)
      .attr("text-anchor", "end")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "13px")
      .style("font-weight", "bold")
      .style("fill", "#444")
      .text(p);

    svg.append("text")
      .attr("x", 130)
      .attr("y", y + cellHeight/2 + 10)
      .attr("text-anchor", "end")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "9px")
      .style("font-style", "italic")
      .style("fill", "#888")
      .text(periodsEn[i]);
  });

  // 필터링된 원소
  const filtered = elements.filter(e => {
    const groupMatch = filterGroup === "전체" || e.group === filterGroup;
    const periodMatch = filterPeriod === "전체" || e.period === filterPeriod;
    return groupMatch && periodMatch;
  });

  // 원소 셀 그리기
  filtered.forEach(e => {
    const x = 150 + (e.col - 1) * (cellWidth + 10);
    const y = 80 + (e.row - 1) * (cellHeight + 10);

    const cell = svg.append("g")
      .style("cursor", "pointer")
      .on("click", () => {
        mutable selectedElement = e.symbol;
      });

    // 배경 (선택 시 강조)
    cell.append("rect")
      .attr("x", x)
      .attr("y", y)
      .attr("width", cellWidth)
      .attr("height", cellHeight)
      .attr("rx", 3)
      .style("fill", selectedElement === e.symbol ? "#f0f0f0" : "#fdfdfd")
      .style("stroke", selectedElement === e.symbol ? "#555" : "#999")
      .style("stroke-width", selectedElement === e.symbol ? 2 : 0.5);

    // 심볼
    cell.append("text")
      .attr("x", x + cellWidth/2)
      .attr("y", y + 40)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "24px")
      .style("font-weight", "bold")
      .style("fill", "#111")
      .text(e.symbol);

    // 한글 이름
    cell.append("text")
      .attr("x", x + cellWidth/2)
      .attr("y", y + 65)
      .attr("text-anchor", "middle")
      .style("font-family", "Apple Myungjo, serif")
      .style("font-size", "13px")
      .style("fill", "#333")
      .text(e.name_kr);

    // 영어 이름
    cell.append("text")
      .attr("x", x + cellWidth/2)
      .attr("y", y + 82)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "9px")
      .style("fill", "#999")
      .style("text-transform", "uppercase")
      .text(e.name_en);
  });

  // 인프라 섹션
  svg.append("text")
    .attr("x", 150)
    .attr("y", 530)
    .style("font-family", "Palatino, Georgia, serif")
    .style("font-size", "13px")
    .style("font-weight", "bold")
    .style("fill", "#555")
    .text("Infrastructure & Ops (기반 기술)");

  svg.append("line")
    .attr("x1", 150)
    .attr("y1", 535)
    .attr("x2", 910)
    .attr("y2", 535)
    .style("stroke", "#ccc")
    .style("stroke-width", 0.5);

  // 인프라 원소
  infra_elements.forEach((e, i) => {
    const x = 260 + i * (cellWidth + 10);
    const y = 545;

    const cell = svg.append("g")
      .style("cursor", "pointer")
      .on("click", () => {
        mutable selectedElement = e.symbol;
      });

    cell.append("rect")
      .attr("x", x)
      .attr("y", y)
      .attr("width", cellWidth - 40)
      .attr("height", cellHeight - 15)
      .attr("rx", 3)
      .style("fill", selectedElement === e.symbol ? "#e8e8e8" : "#f4f4f4")
      .style("stroke", selectedElement === e.symbol ? "#555" : "#888")
      .style("stroke-width", selectedElement === e.symbol ? 2 : 0.5)
      .style("stroke-dasharray", selectedElement === e.symbol ? "none" : "2,2");

    cell.append("text")
      .attr("x", x + (cellWidth - 40)/2)
      .attr("y", y + 30)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "18px")
      .style("font-weight", "bold")
      .text(e.symbol);

    cell.append("text")
      .attr("x", x + (cellWidth - 40)/2)
      .attr("y", y + 52)
      .attr("text-anchor", "middle")
      .style("font-family", "Apple Myungjo, serif")
      .style("font-size", "11px")
      .style("fill", "#333")
      .text(e.name_kr);

    cell.append("text")
      .attr("x", x + (cellWidth - 40)/2)
      .attr("y", y + 67)
      .attr("text-anchor", "middle")
      .style("font-family", "Palatino, Georgia, serif")
      .style("font-size", "8px")
      .style("fill", "#999")
      .text(e.name_en.toUpperCase());
  });

  return svg.node();
}
```

### 선택된 원소 상세 정보

```{ojs}
html`<div style="padding: 25px; background: #f9f9f9; border-left: 4px solid #666; border-radius: 5px;">
  <h2 style="margin-top: 0; color: #222; font-family: Palatino, Georgia, serif;">
    <span style="font-size: 32px; font-weight: bold;">${currentElement.symbol}</span>
    <span style="font-size: 20px; margin-left: 10px;">${currentElement.name_kr}</span>
  </h2>
  <p style="font-size: 14px; color: #999; font-style: italic; margin-top: -10px;">${currentElement.name_en}</p>

  <div style="margin: 20px 0; padding: 15px; background: white; border-radius: 5px;">
    <p style="font-size: 15px; line-height: 1.6; color: #333; margin: 0;">
      ${currentElement.desc}
    </p>
  </div>

  <div style="margin-top: 20px;">
    <h4 style="color: #555; font-family: Palatino, Georgia, serif; margin-bottom: 10px;">분류</h4>
    <span style="display: inline-block; padding: 5px 12px; background: #e8e8e8; border-radius: 3px; margin-right: 10px; font-size: 13px;">
      <strong>족:</strong> ${currentElement.group}
    </span>
    <span style="display: inline-block; padding: 5px 12px; background: #e8e8e8; border-radius: 3px; font-size: 13px;">
      <strong>주기:</strong> ${currentElement.period}
    </span>
  </div>

  <div style="margin-top: 20px;">
    <h4 style="color: #555; font-family: Palatino, Georgia, serif; margin-bottom: 10px;">주요 예시</h4>
    <ul style="margin: 0; padding-left: 20px; color: #555;">
      ${currentElement.examples.map(ex => `<li style="margin: 5px 0;">${ex}</li>`).join('')}
    </ul>
  </div>
</div>`
```

## Column {width="30%"}

### 동위원소 (구현체)

```{ojs}
{
  // 선택된 원소의 동위원소 찾기
  const current_iso = isotopes.find(i => i.element === selectedElement);

  if (!current_iso) {
    return html`<div style="padding: 15px; background: #f9f9f9; border-radius: 5px; margin-bottom: 15px;">
      <h4 style="margin: 0 0 10px 0; color: #555; font-family: Palatino, Georgia, serif;">
        ${currentElement.symbol}의 동위원소
      </h4>
      <p style="color: #999; font-size: 13px; margin: 0;">
        이 원소에 대한 구현체 정보가 등록되지 않았습니다.
      </p>
    </div>`;
  }

  return html`<div style="padding: 15px; background: #f9f9f9; border-radius: 5px; margin-bottom: 15px;">
    <h4 style="margin: 0 0 10px 0; color: #555; font-family: Palatino, Georgia, serif;">
      ${current_iso.element}의 동위원소 (${current_iso.variants.length}개)
    </h4>

    ${current_iso.variants.map(v => `
      <div style="margin: 10px 0; padding: 12px; background: white; border-left: 3px solid #666; border-radius: 3px;">
        <div style="font-weight: bold; color: #333; margin-bottom: 5px;">
          <sup style="font-size: 11px; color: #666;">${v.provider}</sup>
          <span style="font-size: 15px; margin-left: 5px;">${v.symbol.split('-')[0]}</span>
        </div>
        <p style="margin: 3px 0; font-size: 12px; color: #666;">
          ${v.models.join(', ')}
        </p>
        <p style="margin: 3px 0; font-size: 11px; color: #888;">
          ${v.strength}
        </p>
      </div>
    `).join('')}
  </div>`;
}
```

# 화학식 {scrolling="true"}

## {.sidebar}

### 화학식 목록

```{ojs}
{
  const container = d3.create("div")
    .style("padding", "15px");

  container.append("h4")
    .style("color", "#333")
    .style("font-family", "Palatino, Georgia, serif")
    .style("margin-bottom", "15px")
    .text(`AI 아키텍처 패턴 (${formulas.length}개)`);

  const grid = container.append("div")
    .style("display", "grid")
    .style("gap", "10px");

  formulas.forEach((f, i) => {
    const card = grid.append("div")
      .style("padding", "15px")
      .style("background", selectedFormulaIndex === i ? '#e8f4f8' : '#f9f9f9')
      .style("border-left", `4px solid ${selectedFormulaIndex === i ? '#0066cc' : '#999'}`)
      .style("border-radius", "5px")
      .style("cursor", "pointer")
      .style("transition", "all 0.2s")
      .on("click", () => {
        mutable selectedFormulaIndex = i;
      })
      .on("mouseover", function() {
        d3.select(this).style("background", selectedFormulaIndex === i ? '#d8e8f0' : '#f0f0f0');
      })
      .on("mouseout", function() {
        d3.select(this).style("background", selectedFormulaIndex === i ? '#e8f4f8' : '#f9f9f9');
      });

    const header = card.append("div")
      .style("display", "flex")
      .style("justify-content", "space-between")
      .style("align-items", "center")
      .style("margin-bottom", "8px");

    header.append("strong")
      .style("color", "#222")
      .style("font-size", "15px")
      .text(f.name);

    header.append("span")
      .style("padding", "3px 8px")
      .style("background", selectedFormulaIndex === i ? '#0066cc' : '#ccc')
      .style("color", "white")
      .style("border-radius", "3px")
      .style("font-size", "11px")
      .text(f.category);

    card.append("code")
      .style("font-size", "13px")
      .style("color", "#555")
      .style("font-family", "'Courier New', monospace")
      .style("background", "white")
      .style("padding", "5px 8px")
      .style("border-radius", "3px")
      .style("display", "block")
      .text(f.formula);

    card.append("p")
      .style("margin", "8px 0 0 0")
      .style("font-size", "12px")
      .style("color", "#666")
      .style("line-height", "1.4")
      .text(f.description);
  });

  return container.node();
}
```

## Column

### 선택된 화학식 상세

```{ojs}
html`<div style="padding: 25px; background: #f9f9f9; border-radius: 5px;">
  <h2 style="margin-top: 0; color: #222; font-family: Palatino, Georgia, serif;">
    ${currentFormula.name}
  </h2>

  <div style="background: white; padding: 20px; border-radius: 5px; margin: 20px 0; border: 2px solid #0066cc;">
    <code style="font-size: 22px; color: #0066cc; font-family: 'Courier New', monospace; font-weight: bold;">
      ${currentFormula.formula}
    </code>
  </div>

  <div style="background: white; padding: 15px; border-radius: 5px; margin: 15px 0;">
    <p style="font-size: 15px; line-height: 1.7; color: #333; margin: 0;">
      ${currentFormula.description}
    </p>
  </div>

  <div style="margin-top: 20px;">
    <h4 style="color: #555; font-family: Palatino, Georgia, serif; margin-bottom: 10px;">
      구성 원소 (${currentFormula.elements_used.length}개)
    </h4>
    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 10px;">
      ${currentFormula.elements_used.map(sym => {
        const elem = elements.find(e => e.symbol === sym) || infra_elements.find(e => e.symbol === sym);
        return `<button
          onclick="mutable selectedElement = '${sym}';"
          style="
            padding: 10px;
            background: #e8e8e8;
            border: 2px solid #999;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.2s;
            font-family: Palatino, Georgia, serif;
          "
          onmouseover="this.style.background='#d8d8d8'; this.style.borderColor='#666'"
          onmouseout="this.style.background='#e8e8e8'; this.style.borderColor='#999'"
        >
          <div style="font-size: 20px; font-weight: bold; color: #222;">${sym}</div>
          <div style="font-size: 12px; color: #666; margin-top: 3px;">${elem ? elem.name_kr : sym}</div>
        </button>`;
      }).join('')}
    </div>
  </div>

  <div style="margin-top: 20px;">
    <h4 style="color: #555; font-family: Palatino, Georgia, serif; margin-bottom: 10px;">
      활용 사례
    </h4>
    <ul style="margin: 0; padding-left: 20px; color: #555; font-size: 14px;">
      ${currentFormula.use_cases.map(uc => `<li style="margin: 8px 0; line-height: 1.5;">${uc}</li>`).join('')}
    </ul>
  </div>

  <div style="margin-top: 20px; padding: 15px; background: #fff8e6; border-left: 4px solid #ffa500; border-radius: 5px;">
    <strong style="color: #cc8800; font-size: 13px;">예시 구현:</strong>
    <p style="margin: 8px 0 0 0; color: #666; font-size: 14px;">
      ${currentFormula.example_impl}
    </p>
  </div>
</div>`
```
